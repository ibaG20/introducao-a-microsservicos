spring:
  application:
    name: payroll-api
server:
  port: 8100
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URL:http://localohost:8761/eureka}
  server:
    wait-time-in-ms-when-sync-empty: 0